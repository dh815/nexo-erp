import { ComingSoon } from '../components/ComingSoon';

export default function Configuracoes() {
  return (
    <ComingSoon
      crumb="Sistema"
      title="Configurações"
      subtitle="Dados da empresa, aparência, usuários e permissões"
      note="Entra assim que o cadastro de empresa (modelo multi-empresa) estiver validado."
    />
  );
}
